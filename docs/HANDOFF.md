# SatoTrip 引継ぎ資料（2026-07-11 時点）

新しい開発セッション向けの現状サマリ。**必ず先に ルートの `CLAUDE.md`（開発の罠・規約）を読むこと。**

## 現状（本番稼働中）

| 層 | 場所 | 備考 |
|---|---|---|
| フロント | Vercel https://sato-trip-ai.vercel.app （main 自動デプロイ） | `VITE_API_BASE_URL` はビルド時焼き込み |
| バックエンド | Render https://satotrip-ai-backend.onrender.com （main / render.yaml） | 無料枠・15分で休眠・/health |
| DB | Supabase Postgres | **スポットは鹿児島の7件のみ**（他エリアは生成不可） |

**動作実証済み**: 登録→ログイン→AIプラン生成(実Gemini)→紋章付きプラン詳細、まで本番でE2E確認済み。

## 完了フェーズ（merged PR #1〜#5）

1. **PR#1**: 全体監査（`健康診断レポート.md`）＋コア修正（Geminiモデル/テーマ0件/デバッグ除去/シークレット追跡解除）＋新機能（お気に入り/設定/アバター/パスワードリセット/Googleログイン/Stripe基盤）＋インフラ（TLS/nginx/非root/レート制限）＋Renderデプロイ一式
2. **PR#2**: UI刷新「旅の同人誌 × 地形図」— デザイントークン(`src/design/tokens.*`)、ルート紋章(`src/design/crest.ts`・**後方互換厳守**)、SVG等高線ランディング、織物ローダー、単一ページ作成フォーム（下書き自動保存・趣味引き継ぎ `?hobby=`）、`#/design-preview`
3. **PR#3**: 生成中のイベントループ閉塞→Render 502 の修正（asyncio.to_thread）
4. **PR#4**: CLAUDE.md
5. **PR#5**: UXバグ修正（交通手段切替でルート再取得・番号入りマーカー・宿泊不要トグル）＋商用監査対応（**写真プロキシ `/api/spots/photo?ref=`**・Stripe完全化 webhook3種＋`POST /api/payments/portal`・登録時規約同意・AI免責表示・本番/docs無効化）

商用利用監査レポート（詳細版）は**オーナーの手元のみ**（機微情報のためリポジトリ非コミット）。要点は下記タスクに反映済み。

## 未完タスク（優先度順）

### A. オーナー操作待ち（コード外）
- [ ] **Places APIキーのローテーション＋API/リファラ制限**（キーは過去に全ユーザーへ露出済み・最優先）
- [ ] 特定商取引法表記の実値記入（`pages/LegalPages.tsx` の PLACEHOLDER 15箇所）＋解約手順の明記
- [ ] 管理者パスワード変更（git履歴に平文が残存）
- [ ] git履歴のシークレット抹消（`git filter-repo` で `backend/.env.production`・`login.json`）
- [ ] Render に `REDIS_URL`（レート制限の共有ストア）／SMTP設定（パスワードリセットメール）

### B. コード・短期
- [ ] 設定画面に**解約ボタン**（`POST /api/payments/portal` を呼んで返却URLへ遷移。API実装済み・UI未着手）
- [ ] **退会（アカウント削除）機能**（個人情報保護法対応。カスケード削除＋Stripe解約連動）
- [ ] 認証のIPレート制限＋登録のメール確認 or CAPTCHA（無料生成枠×大量アカウントのコスト増幅対策）
- [ ] 既存DB/プランJSONのキー入りURL一括クレンジングスクリプト（応答時変換で当面は無害化済み）
- [ ] JWT失効機構（ログアウト・パスワード変更で旧トークン無効化）

### C. 事業・中期
- [ ] **スポットデータの全国拡充**（最重要。現状鹿児島7件。管理画面の一括追加＋収集パイプライン活用。デフォルト評価4.0の一律付与は景表法リスクなので null 化とセットで）
- [ ] 宿泊の空室連動＋アフィリエイト予約導線（楽天トラベル空室API等の選定から。生成時案内と実際の空きのギャップ解消がUX要件）
- [ ] 公共交通機関のダイヤ考慮スケジューリング（現在は driving 近似＋係数）
- [ ] プラン詳細ページの新デザイン統一（現在は紋章ヘッダーのみ新化。DESIGN_PROPOSAL §8 の段階移行）
- [ ] OSRMデモサーバ→自前/商用API、OSM公式タイル→商用タイルへの切替（提供元ポリシー対応）
- [ ] YouTube収集データの保存方針是正（video IDのみ保存・タイトルだけのAI"要約"廃止）
- [ ] WebGL等高線ヒーロー v2（現行はSVG+CSS版。モックは `docs/ui-redesign/mockup.html`）

## 作業ルール（このプロジェクトでの慣行）

- 作業ブランチ: `claude/repository-overview-dx41mh` を **origin/main から作り直して**使う → push → draft PR → オーナー了承のもと main へマージ（マージ権限はオーナーが都度明示）
- コミットに dist/node_modules を**絶対に含めない**（CLAUDE.md 参照）
- 検証: tsc 0エラー・vite build・バックエンドは ast 構文＋可能なら本番 curl。UI変更はスクリーンショットをオーナーへ送る
- 大きめの独立タスクはサブエージェント並列（担当ファイル限定・git操作禁止・レビュー統合コミット）が有効だった
- UX方針: 重複入力の排除・入力しながら修正可能・下書き自動保存（オーナー明示の要件）
